---
"@vanya2h/store-react": minor
---

Initial release. React bindings for `@vanya2h/store`: `useStoreValue` (subscribes to a key, returns `IPendingStatus<T>`) and `useStoreCache` (returns the underlying `ICache<T>` for `set` / `next` / `fresh` access).
