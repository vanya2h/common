---
"@vanya2h/async-actions": minor
---

Add new `@vanya2h/async-actions` package with action and action-set primitives (`ActionSingle`, `ActionsSet`, `IAction`, `IActionSingle`, `IActionState`, `IActionStatus`, `IActionEvents`, `IActionSetEvents`, `InferActionInput`, `InferActionOutput`, `InferActionsResult`) built on `tiny-typed-emitter` and `immutable.List`.
