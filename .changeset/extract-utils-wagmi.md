---
"@vanya2h/utils-wagmi": minor
---

Add new `@vanya2h/utils-wagmi` package with wagmi/viem utilities for accounts, public clients and wallet clients (`getAccountByStatus`, `getConnectedAccount`, `getRequiredAccount`, `isContract`, `getPublicClientWithChain`, `getWalletClientWithChain`, plus chain-id inference helpers and a `WalletIsRequiredError`).
