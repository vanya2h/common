---
"@vanya2h/utils": minor
---

Add `jsonSerializer` to `@vanya2h/utils/common` for round-tripping JSON values that contain `bigint` and `Date`. Also exports the supporting types `ISerializer`, `JsonValue`, `JsonObject`, `JsonArray`, and `JsonPrimitive`.
